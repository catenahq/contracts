# Files exempt from the banned-word scan in check-unicode.mjs.
# Format: <path relative to the scan root> -- <reason>
#
# Unicode hygiene still applies to a listed file. A listed file that names
# no banned system any more fails the gate, so delete the entry when the
# file is cleaned rather than leaving it here.
#
# Seeded 2026-09-11, when the gate stopped carrying a hand-maintained copy
# of the word list and started reading banned-words.json. The manifest
# lists 21 tokens; the copy had 11 and matched with \b, which anchors in
# the wrong place for cal.com and vault.sops.yml.
#
# BOTH ARE PUBLISHED LEGAL TEXT, and the most serious entries in any debt
# file in this workspace. They commit Catena, in the client's copy, to a
# restore flow the product stopped shipping: `restore.sh` was replaced by
# `catena recover`. An obligation written against a system that does not
# exist is not a small doc bug. Fixing it is a legal-text change, not an
# editing pass, which is why it is recorded here rather than done in
# passing.

legal/data-export-and-termination.md -- names restore.sh
legal/sla.md -- names restore.sh
